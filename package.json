{
  "name": "spaceduck",
  "version": "1.0.0",
  "description": "SpaceDuck — космическая игра-исследование",
  "private": true,
  "scripts": {
    "serve": "npx http-server www -p 8080 -c-1",
    "android:sync": "npx cap sync android",
    "android:build": "npx cap copy android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  }
}